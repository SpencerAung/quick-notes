function CreateBucket() {
  const showForm = () => {
  }

  return (
    <div>
      <button
      className="bg-indigo-500 text-white px-4 py-2"
      onClick={showForm}>Create Bucket</button>
    </div>
  )
}

export default CreateBucket;
